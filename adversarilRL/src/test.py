import numpy as np
import random
import time
import re
# 1. randomly generate a grid, all elements are 0
# 2. mark the door and the prisoner's coordinates as 1
# 3. use DFS to generate a path between door and prisoner

x = 'checkpoint/single/actor_prisoner'
y = x[10:]

print(y)